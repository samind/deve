{
  "environment": "production",
  "run_list": [
   "recipe[base::grub]",
   "recipe[base::selinux]",   
   "recipe[base::network]",
   "recipe[base::ssh]",
   "recipe[apache]"
   ],
    "automatic": {
    "ipaddress": "deve"
  }
}


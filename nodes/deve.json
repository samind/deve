{
  "environment": "production",
  "run_list": [
   "recipe[base::grub]",
   "recipe[base::selinux]",   
   "recipe[base::network]",
   "recipe[base::ssh]"
   ],
    "automatic": {
    "ipaddress": "deve"
  }
}

